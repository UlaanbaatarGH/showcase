# Showcase

Web app for sharing organised collections of items with custom properties and pictures, structured as a folder tree with fine-grained access control.

See `doc/fixes.md` for the full functional spec (FIX500–FIX520).

## Develop

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
